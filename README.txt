This is the interpreter for the LAIka programming language

Compile:
    $ deno compile -A --target x86_64-pc-windows-msvc laika.ts

To run command-line interpreter:
    $ .\laika.exe

To run test files:
    $ .\laika.exe .\tests\test.lai